# Kyle’s Guinea Pig Club

A responsive, illustrated guinea pig website with rotating facts and a name generator.

Open `public/index.html` in a browser, or run:

```sh
python3 -m http.server 8000 --directory public
```

Then visit http://localhost:8000. No build or dependencies required. Fonts use Google Fonts with local fallbacks. All illustrations are included in the HTML.
